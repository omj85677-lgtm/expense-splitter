# expense-splitter
Splits shared expenses and finds the minimum transactions to settle all debts using a greedy algorithm. Uses @dataclass for clean data models, defaultdict for O(E×S) balance aggregation, and an O(N log N) debt-minimization function. Fully modular, PEP 8 compliant, with type hints and docstrings throughout.
